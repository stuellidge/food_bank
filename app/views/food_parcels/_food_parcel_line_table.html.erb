<table id="parcel_lines_table">
  <thead>
    <tr><th>Product Code</th><th>Product Name</th><th>Quantity</th></tr>
  </thead>
  <tbody>
    <% @food_parcel.food_parcel_lines.each_with_index do |line, index| %>
      <%= render :partial => 'food_parcel_line', :locals => { :food_parcel_line => line, :index => index } %>
    <% end %>
  </tbody>
</table>